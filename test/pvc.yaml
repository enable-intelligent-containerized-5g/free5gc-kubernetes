apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-hostpath-volume-claim
spec:
  selector:
    matchLabels:
      type: local
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
